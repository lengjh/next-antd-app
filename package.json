{
  "name": "my-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "node server.js",
    "build": "rm -fr build && next build",
    "start": "rm -fr build && next build && cross-env NODE_ENV=production node server.js",
    "export": "rm -fr build && next build && next export",
    "demo": "npm run export && serve out"
  },
  "browserslist": [
    "> 0.3%",
    "last 7 versions",
    "Android >= 4",
    "Firefox >= 20",
    "iOS >= 8"
  ],
  "dependencies": {
    "@zeit/next-less": "^1.0.1",
    "antd": "^4.0.2",
    "babel-plugin-import": "^1.13.0",
    "babel-polyfill": "^6.26.0",
    "cross-env": "^7.0.2",
    "cross-fetch": "^3.0.4",
    "less": "^3.11.1",
    "micro-cors": "^0.1.1",
    "next": "^9.2.2",
    "next-compose-plugins": "^2.2.0",
    "null-loader": "^3.0.0",
    "postcss-flexbugs-fixes": "^4.2.1",
    "postcss-preset-env": "^6.7.0",
    "react": "16.13.0",
    "react-dom": "16.13.0",
    "swr": "^0.1.18",
    "tailwindcss": "^1.2.0"
  }
}